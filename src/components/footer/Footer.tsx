import { Typography } from '@mui/material';

export const Footer = () => (
  <footer className="flex-1 flex justify-center items-end p-4">
    <Typography variant="body2" className="opacity-70">
      Horváth Balázs
    </Typography>
  </footer>
);
